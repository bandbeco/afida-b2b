<nav class="bg-white border-b-2 border-black">
  <div class="mx-auto max-w-7xl px-2 sm:px-6 lg:px-8">
    <div class="relative flex h-16 items-center justify-between">
      <div class="flex flex-1 items-center justify-center sm:items-stretch sm:justify-start">
        <div class="flex flex-shrink-0 items-center">
        <%= link_to root_path do %>
          <%= render "shared/logo" %>
        <% end %>
        </div>
        <div class="hidden sm:ml-6 sm:block">
          <div class="flex space-x-4 lg:text-lg justify-center">
            <%= render 'shared/navlinks' %>
          </div>
        </div>
      </div>

      <div class="absolute inset-y-0 right-0 flex items-center pr-2 sm:static sm:inset-auto sm:ml-6 sm:pr-0">
        <!-- Profile dropdown -->
        <div class="relative ml-3">
          <div class="max-sm:hidden relative flex text-sm">
            Logged in as&nbsp;<%= link_to current_user.formatted_name, edit_user_path(current_user) %>
          </div>

          <div>
            <%= link_to "logout", destroy_user_session_path, data: {
              turbo_method: :delete
              },
              class: "relative flex text-sm"
            %>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Mobile menu, show/hide based on menu state. -->
  <div class="sm:hidden" id="mobile-menu">
    <div class="flex justify-center">
      <%= render 'shared/navlinks' %>
    </div>
  </div>
</nav>
