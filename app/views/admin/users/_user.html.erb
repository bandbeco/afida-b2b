<tr id="<%= dom_id user %>" class="even:bg-gray-50">
  <td class="whitespace-nowrap py-4 pl-4 pr-3 text-sm font-medium text-gray-900 sm:pl-3">
    <%= link_to user.formatted_name, edit_user_path(user) %>
  </td>

  <td class="whitespace-nowrap px-3 py-4 text-sm text-gray-500">
    <%= user.email %>
  </td>

  <td class="whitespace-nowrap px-3 py-4 text-sm text-gray-500">
    <%= user.role %>
  </td>

  <td class="whitespace-nowrap px-3 py-4 text-sm text-gray-500">
    <%= user.invited_by&.formatted_name %>
  </td>

  <td class="whitespace-nowrap px-3 py-4 text-sm text-gray-500">
    <%= local_time(user.invitation_sent_at) if user.invited? %>
  </td>

  <td class="whitespace-nowrap px-3 py-4 text-sm text-gray-500">
    <%= local_time(user.invitation_accepted_at) if user.accepted_invitation? %>
  </td>

  <td class="whitespace-nowrap px-3 py-4 text-sm text-gray-500">
    <% unless user.invited? %>
      <%= form_for(user, url: invitation_path(user), html: { method: :post }) do |f| %>
        <%= f.hidden_field :user_id, value: user.id %>
        <%= f.submit "Send Invitation Email",  class: "btn-primary" %>
      <% end %>
    <% end %>
  </td>

  <td class="whitespace-nowrap px-3 py-4 text-sm text-gray-500">
    <% if user.price_list_items.any? %>
      <%= link_to "Update Price List", user_price_list_items_path(user) %>
    <% end %>
  </td>
</tr>
