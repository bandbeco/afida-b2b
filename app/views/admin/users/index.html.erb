<% content_for :title, "Users" %>

<div class="card bg-neutral text-neutral-content my-4">
  <div class="card-body p-4">
    <h1 class="card-title text-3xl font-bold italic uppercase">
      USERS
    </h1>
  </div>
</div>

<div class="flex justify-between items-center mb-4 gap-4">
  <%= form_with url: admin_users_path, method: :get, data: { controller: "search-form", turbo_frame: "users_list" }, class: "flex-grow max-w-2xl" do |form| %>
    <label class="input input-bordered flex items-center gap-2">
      <%= form.text_field :query,
        value: params[:query],
        placeholder: "Search by name, company or email",
        class: "grow",
        type: "search",
        data: { action: "input->search-form#search" } %>
      <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 16 16" fill="currentColor" class="w-4 h-4 opacity-70"><path fill-rule="evenodd" d="M9.965 11.026a5 5 0 1 1 1.06-1.06l2.755 2.754a.75.75 0 1 1-1.06 1.06l-2.755-2.754ZM10.5 7a3.5 3.5 0 1 1-7 0 3.5 3.5 0 0 1 7 0Z" clip-rule="evenodd" /></svg>
    </label>
  <% end %>

  <div>
    <%= link_to new_admin_user_path, class: "btn btn-primary gap-2" do %>
      Create new user
      <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="size-6">
        <path stroke-linecap="round" stroke-linejoin="round" d="M12 9v6m3-3H9m12 0a9 9 0 1 1-18 0 9 9 0 0 1 18 0Z" />
      </svg>
    <% end %>
  </div>
</div>

<%= turbo_frame_tag "users_list" do %>
  <div class="card bg-base-100 shadow-xl mt-4">
    <div class="card-body p-0">
      <div class="overflow-x-auto">
        <table class="table table-zebra">
          <thead>
            <tr>
              <th>Name</th>
              <th>Company</th>
              <th>Email</th>
              <th>Role</th>
              <th>
                <span class="sr-only">Invite</span>
              </th>
              <th>
                <span class="sr-only">Update price list</span>
              </th>
            </tr>
          </thead>
          <tbody>
            <% @users.each do |user| %>
              <%= render user %>
            <% end %>
          </tbody>
        </table>
      </div>
    </div>
  </div>
<% end %>
