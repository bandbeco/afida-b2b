<div class="grid grid-cols-1 gap-y-4 sm:grid-cols-2 sm:gap-x-6 sm:gap-y-10 lg:grid-cols-3 lg:gap-x-8 justify-end">
  <%= form.fields_for :order_items do |order_item| %>
    <% price_list_item = price_list_items.find { |p| p.product_id == order_item.object.product_id } %>
    <% product = price_list_item.product %>

    <div class="group relative flex flex-col rounded-lg border-gray-800 border-4 bg-white">
      <div class="aspect-h-4 aspect-w-3 sm:aspect-none group-hover:opacity-75 sm:h-96">
        <% if product.picture.attached? %>
          <%= image_tag product.picture, class: "h-full w-full object-cover object-center sm:h-full sm:w-full" %>
        <% end %>
      </div>
      <div class="flex flex-1 flex-col space-y-2 p-4">
        <h3 class="text-xl font-medium text-gray-900">
          <%= link_to product.name, product %>
          <p class="text-sm text-gray-500">
            SKU: <%= product.sku %>
          </p>
        </h3>

        <p class="text-sm text-gray-500"><%= product.description %></p>
        
        <div class="flex flex-1 flex-col justify-end text-sm">
          <p>Colour: <%= product.colour %></p>

          <% if width = product.width_in_mm %>
            <p>Volume: <%= width %> ml</p>
          <% end %>

          <% if height = product.height_in_mm %>
            <p>Height: <%= height %> mm</p>
          <% end %>

          <% if depth = product.depth_in_mm %>
            <p>Depth: <%= depth %> mm</p>
          <% end %>

          <% if diameter = product.diameter_in_mm %>
            <p>Diameter: <%= diameter %> mm</p>
          <% end %>

          <%= order_item.hidden_field :product_id %>
          <%= order_item.hidden_field :unit_price %>
        </div>

        <p class="text-lg font-medium text-gray-900">
          <% if price_list_item.price.present? %>
            &pound;<%= price_list_item.price %>
          <% else %>
            Price To Be Confirmed
          <% end %>
        </p>

        <div>
          <%= order_item.label :quantity %>
          <%= order_item.number_field :quantity, min: 0, value: 0, class: "rounded-md border border-gray-400 outline-none px-3 py-2 mt-2 w-full" %>
        </div>
      </div>
    </div>
  <% end %>
</div>

<div class="mt-5 grid gril-cols-1 justify-items-end">
  <div>
    <%= form.submit "Proceed to checkout", class: "block bg-blue-500 text-white rounded-md px-3 py-2 mt-2" %>
  </div>
</div>
