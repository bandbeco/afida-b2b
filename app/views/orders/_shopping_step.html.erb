<div class="bg-black text-white italic font-extrabold grid text-3xl font-impact rounded border-black border-2 p-4 my-4">
  <h2>
    <%= category.name.upcase %>
  </h2>
</div>

<div class="grid grid-cols-1 gap-x-6 gap-y-10 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 xl:gap-x-8">
  <% price_list_items.each do |price_list_item| %>
    <%= form.fields_for :order_items, @order.order_items.build(product_id: price_list_item.product_id, unit_price: price_list_item.price) do |f| %>
      <%= render "order_items/order_item",
        form: f,
        price_list_item: price_list_item
      %>
    <% end %>
  <% end %>
</div>
