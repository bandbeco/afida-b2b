<div class="card bg-base-100 shadow-sm mt-10 pb-4">
  <div class="card-body">
    <h3 class="card-title">Billing address</h3>

    <div class="grid grid-cols-1 gap-4 sm:grid-cols-3">
      <%= form.fields_for :billing_address do |billing_address| %>
        <div class="form-control sm:col-span-3">
          <%= billing_address.label :street_number_and_name, "Address", class: "label" %>
          <%= billing_address.text_field :street_number_and_name, autocomplete: "street-address", class: "input input-bordered w-full" %>
        </div>

        <div class="form-control">
          <%= billing_address.label :post_town, "City", class: "label" %>
          <%= billing_address.text_field :post_town, autocomplete: "address-level2", class: "input input-bordered w-full" %>
        </div>

        <div class="form-control">
          <%= billing_address.label :postcode, "Postcode", class: "label" %>
          <%= billing_address.text_field :postcode, autocomplete: "postal-code", class: "input input-bordered w-full" %>
        </div>
      <% end %>
    </div>
  </div>
