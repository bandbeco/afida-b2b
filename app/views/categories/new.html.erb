<div class="container mx-auto px-4 py-8">
  <div class="prose lg:prose-xl mb-6">
    <h1 class="text-3xl font-bold">New Category</h1>
  </div>

  <%= render "form", category: @category %>

  <div class="mt-6">
    <%= link_to "Back to categories", categories_path, class: "btn btn-outline" %>
  </div>
</div>
